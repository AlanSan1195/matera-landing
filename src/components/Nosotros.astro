---
import We from "./We.astro";
import { type Lang } from "@/i18n/utils";

interface Props {
  id?: string;
  lang: Lang;
}
const { id, lang } = Astro.props;
---
<section
  class="landing-section w-screen h-screen text-center"
>
  <We title="nosotros" id="nosotros" lang={lang} />
</section>
