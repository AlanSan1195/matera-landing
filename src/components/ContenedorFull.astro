---


const { color, title, subtitle, id, showFooterContent } = Astro.props;
const textColor = `text-${color}`;

---
<section
  class="landing-sec bg-zinc-900 h-screen w-screen text-center overflow-hidden "
  data-header-color={color}
  id={id}
> 
</section>