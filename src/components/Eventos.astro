---
const { color, title, subtitle, id, showFooterContent } = Astro.props;
const textColor = `text-${color}`;
import Portada from "./Portada.astro"
import Caurosel from "./Caurosel.astro"
---

<section
  class="landing-section bg-zinc-900   w-screen text-center overflow-hidden  "
  data-header-color={color}
  id={id}
>

<Caurosel id="eventos"></Caurosel>
</section>

